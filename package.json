{
  "name": "@mybricks/rocker-commons",
  "version": "0.0.2",
  "description": "The commons for rocker frameworkers.",
  "author": "CheMingjun",
  "scripts": {
    "build": "npm run clean && (tsc || true)",
    "clean": "rimraf *.js ./src/**/*.js ./src/**/*.d.ts ./test/**/*.js ./vbuilder-demo/**/*.js",
    "dev": "npm run build && npm run start & node test/test.js",
    "start": "tsc -w"
  },
  "repository": {
    "type": "git",
    "url": "http://gitlab.hellobike.cn/Power/rocker/common.git"
  },
  "license": "MIT",
  "directories": {
    "doc": "doc"
  },
  "devDependencies": {
    "@types/node": "^13.9.0",
    "rewire": "^3.0.2",
    "rimraf": "^2.6.2",
    "typescript": "^3.8.3"
  },
  "main": "./index.js"
}
